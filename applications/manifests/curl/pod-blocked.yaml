apiVersion: v1
kind: Pod
metadata:
  name: curl-blocked
  labels:
    app: curl-blocked
spec:
  containers:
  - image: curlimages/curl
    command:
      - sleep
      - "3600"
    imagePullPolicy: IfNotPresent
    name: curl
  restartPolicy: Always